function identifyType(value) {
  let type = typeof value;
  console.log(`Type: ${type}`);
}

identifyType(1);
identifyType("JavaScript");
identifyType(false);
identifyType(undefined);
identifyType(null);
